apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: postiz
spec:
  interval: 5m
  chart:
    spec:
      chart: postiz-app
      version: '*'
      sourceRef:
        kind: HelmRepository
        name: postiz
        namespace: flux-system
      interval: 5m
  valuesFrom:
    - kind: Secret
      name: postiz-values
